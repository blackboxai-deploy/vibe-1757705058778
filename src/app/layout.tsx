import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'
import { Toaster } from '@/components/ui/sonner'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'مرشدك العقاري الشامل - Your Comprehensive Real Estate Guide',
  description: 'AI-Powered Real Estate Scout platform for the Arab region, Turkey, and Iraq. Comprehensive marketplace with advanced analytics.',
  keywords: 'real estate, property, AI analytics, Arab countries, Turkey, Iraq, property valuation, market analysis',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  )
}